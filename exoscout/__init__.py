"""ExoScout - autonomous TESS candidate triage & follow-up planning agent.

Week-1 MVP: two deterministic tools (light-curve fetch/vetting + archive
novelty check) exposed as pure, structured-return functions so they can later
be registered as callable tools for an LLM orchestrator.
"""

__version__ = "0.1.0"
