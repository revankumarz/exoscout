# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Data caches / downloads (keep labeled_tois.csv, ignore the DB + downloads)
data/*.db
data/*.npz
data/*.log
mastDownload/
*.fits
.exoscout_cache/

# Trained model weights (reproducible via exoscout/ml/train.py). Keep metrics.
models/*.pt

# Streamlit
.streamlit/secrets.toml

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
